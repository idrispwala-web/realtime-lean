# realtime-lean

Before the first call to any realtime / rt_* tool, read `skills/realtime-lean/SKILL.md` and follow it. Look names up in `skills/realtime-lean/reference/` instead of calling search or describe tools. Always pass `select` (queries) or `fields` (GET calls).
