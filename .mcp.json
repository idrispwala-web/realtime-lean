{
  "mcpServers": {
    "realtime": {
      "command": "node",
      "args": [
        "${CLAUDE_PLUGIN_ROOT}/mcp/server.mjs"
      ],
      "env": {
        "RT_API_KEY": "${RT_API_KEY}"
      }
    }
  }
}
